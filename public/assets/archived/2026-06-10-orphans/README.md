# Archived orphan assets — 2026-06-10

Moved here from the active asset folders during the package/129 workstream-F image
audit + package/133 brand-asset audit. Each had **zero references** anywhere in the
site at archive time. Kept (not hard-deleted) for history; safe to remove entirely
later if never reinstated.

- `band_cushion_study.{jpg,webp}` — early kit study, superseded by `cushion_family.*`.
- `color_palette.png` — duplicate of the in-use `color_palette.{jpg,webp}` (PNG copy unused).
- `logo_{A,B,D,E}.{jpg,webp}` — logo-vote candidates; **Logo C won** and is the live mark
  across header / favicon / footer / press. A/B/D/E retained here for the record.

To reinstate any asset, `git mv` it back to its original folder (`public/assets/` or
`public/assets/logos/`).
