# ============================================================================
# WTP_TEST_FLAG: ON (live). The three /price/<n>/ variants serve their A/B/C
# pages directly per package/112. Flipped 2026-06-10T14:20 EDT per Mike's
# direct instruction (item 3 "WTP flip — merge"). Signal collected through
# pre-launch waitlist conversion on each variant.
# To re-disable: add back the three `302!` lines below the comment.
# See plumb-coordination package/112.
# ============================================================================

# TESTER_FUNNEL_FLAG: OFF (dormant pre-G1). Flip = delete the /test/ line in one PR
# per package/111 (recruitment release is part of the G1 trigger).
/test/* / 302!
